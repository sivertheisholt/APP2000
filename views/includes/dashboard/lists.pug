- var lists = lists;
- var posters = posters
script.
    let userId = !{userId};
//-
    Viser en liste over lister til brukeren
    Authors: Sivert - 233518, Ørjan - 233530
p #{__("DASHBOARD_LISTS_TITLE")}
    .uk-inline
        .uk-flex
            input.uk-input(type='text' name='username' placeholder=`${__('LIST_NAME')}` id="newListName")
            button.uk-button.uk-button-default.uk-margin-right#newList #{__("NEW_LIST_BUTTON")}
        #createListOutput.uk-margin-top
    .uk-margin 
        .uk-grid.uk-child-width-1-3.uk-grid-match#lists(uk-scrollspy="cls: uk-animation-slide-bottom; target: .uk-card; delay: 300; repeat: false")
            each item in lists
                a(href=`/${urlPath}/list/lists/${item.id}` class='uk-margin-bottom')
                    .uk-card.uk-card-default.dashboard-favorite-padding
                        .uk-card-media-top
                            img(src=`https://www.themoviedb.org/t/p/w600_and_h900_bestv2` onerror="this.onerror=null; this.src='/images/filmatory_default_poster.png'" alt='')
                        .uk-card-body
                            h3.uk-card-title= item.name
                            p= `Movies: ${item.movies.length}`
                            p= `Shows: ${item.tvs.length}`